# Felipe de Carvalho Figueiredo — Academic Profile

An editorial, research-oriented personal website built with Next.js and designed
for static deployment on Vercel Hobby.

## Local development

```bash
pnpm install
pnpm dev
```

Run the production check with:

```bash
pnpm build
```

## Verified profile content

Identity, profile facts, research interests, current manuscripts, presentations,
experiences, projects, teaching, service, languages, and CV download paths live
in `lib/site.ts`. Keep claims conservative and change a content item to
`status: "draft"` whenever it is not ready to publish.

Before expanding the profile, verify:

- exact institutional role or employer wording;
- dates and host institution for international rotations;
- public Lattes, ORCID, GitHub, or institutional profile URLs;
- publication metadata and DOI;
- current ECFMG/USMLE status;
- project stage and validation status.

Only confirmed public links belong in `siteProfile.links` and `sameAs` structured
data.

## Blog

Blog posts live in `content/blog/*.md`. The filename becomes the public URL slug,
so `content/blog/example-post.md` is published at `/blog/example-post`.

Use this validated frontmatter:

```yaml
title: "A descriptive title"
summary: "A concise search and index description of at least 30 characters."
date: "2026-07-29"
language: "en"
status: "published"
```

`language` defaults to `en` and `status` defaults to `published` when omitted.
Use `status: "draft"` to keep a file out of the blog index, sitemap, and static
route generation. The build fails when required frontmatter is incomplete or
malformed. Legacy `/notes` URLs permanently redirect to `/blog`.

## Assets

- Portrait: `public/images/felipe-portrait.jpg`
- Favicon/manifest icon: `public/icon.svg`
- Open Graph image: generated by `app/opengraph-image.tsx`
- Current CV downloads: `public/cv/Felipe_de_Carvalho_Figueiredo_CV.pdf`
  and `public/cv/Felipe_de_Carvalho_Figueiredo_CV.docx`

The online CV is assembled from verified content in `lib/site.ts`. Replace both
files together when the source CV changes, then update the online facts in the
same commit.

## Search launch checklist

1. Add and verify `https://felipef.com` in Google Search Console.
2. Submit `https://felipef.com/sitemap.xml`.
3. Inspect the homepage, Portuguese profile, and each published note.
4. Keep the same full name and site URL on confirmed professional profiles.
5. Re-run structured-data, accessibility, and performance checks after material
   content updates.
